import React from 'react';
import { Text, StyleSheet } from 'react-native';

const ChatListScreen = () => {
  return <Text>ChatListScreen</Text>;
};

const styles = StyleSheet.create({});

export default ChatListScreen;
